version: "2"

linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - gosec
    - ineffassign
    - unused
    - misspell

formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/jhuiting/chargebee-cli
